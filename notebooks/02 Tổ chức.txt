project/
│
├── data/
│   ├── raw.csv
│   └── clean.csv
│
├── src/
│   ├── eda.py
│   ├── cleaning.py
│   ├── features.py
│   ├── train.py
│   └── utils/
│
└── notebooks/
    ├── 01_eda.ipynb
    └── 02_cleaning.ipynb




📂 project
 ├── data_loader.py        # load csv, split train/test
 ├── data_cleaning.py      # clean data
 ├── features.py           # feature engineering
 ├── eda.py                # exploratory plots/stats
 ├── train.py              # train model
 ├── evaluate.py           # metrics, visualization
 └── models.py             # khai báo và cấu hình mod



 
from my_eda import plot_histogram
from my_cleaning import clean_data

Mẫu chat gpt

import pandas as pd

def load_data():
    return pd.read_csv("data/raw.csv")

def clean(df): so sánh có tham số df và không có
    df = df.dropna()
    df = df[df['price'] > 0]
    return df

if __name__ == "__main__":  <====
    df = load_data()
    df = clean(df)
    df.to_csv("data/clean.csv", index=False)